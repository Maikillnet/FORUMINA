{
  "name": "forum-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch-path=server.js --watch-path=app.js --watch-path=db.js --watch-path=config --watch-path=controllers --watch-path=middleware --watch-path=routes --watch-path=utils server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "express-rate-limit": "^8.6.1",
    "jsonwebtoken": "^9.0.2",
    "lowdb": "^7.0.1"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^4.1.10"
  }
}
